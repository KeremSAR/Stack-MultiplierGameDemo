using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PoleCounter : MonoBehaviour
{
    public Text text;
    private int ten;
    private int five;
    /*void Start()
    {
        if (PlayerMovement.instance.mult2 == true)
        {
            text.text = "+" + (PlayerMovement.instance.list.Count * 2).ToString();
        }
        else if (PlayerMovement.instance.mult3 == true)
        {
            text.text = "+" + (PlayerMovement.instance.list.Count * 3).ToString();
        }
        else if (PlayerMovement.instance.mult4 == true)
        {
            text.text = "+" + (PlayerMovement.instance.list.Count * 4).ToString();
        }
        else if (PlayerMovement.instance.plus5 == true)
        {
            text.text = "+5";
        }
        else if (PlayerMovement.instance.plus10 == true)
        {
            text.text = "+10";
        }
    }*/
}
